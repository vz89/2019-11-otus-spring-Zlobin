Spring:
  data:
    mongodb:
      database: test3
      host: localhost
      port: 27017
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/librarytest?serverTimezone=Europe/Moscow&useSSL=false
    username: root
    password: root
    schema: classpath:schema.sql
    initialization-mode: always
  batch:
    job:
      enabled: false
    initialize-schema: always
